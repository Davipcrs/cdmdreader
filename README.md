# cdmdreader

A easy markdown reader for linux and windows.  

Run the following command in the project root to add the platform before building:

```shell
flutter create --platforms=windows .
flutter create --platforms=linux .
```
